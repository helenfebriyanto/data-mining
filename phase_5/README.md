# Phase 5 Dashboard — PaySim Banking Discovery

This folder contains a Python Dash dashboard for the Phase 5 Knowledge Presentation deliverable. The dashboard is written in business English and is designed for a non-technical audience.

## What the dashboard shows

- Executive overview of fraud concentration and the review queue.
- Customer-transaction segmentation based on K-Means clusters.
- Cluster characteristics translated into business value.
- Association rules translated into plain-English findings.
- Anomaly typology, investigation category, and risk-score validation.
- Suspicious transaction queue from the Phase 4 export.
- Data quality findings that explain how the PaySim dataset should be interpreted.

## Why K-Means is used as the main segmentation reference

K-Means is used as the main customer segmentation lens because it creates four stable and interpretable segments. Direct HDBSCAN is useful as a comparison but labels too many rows as noise, while BIRCH + HDBSCAN is more useful as an outlier cross-reference for Phase 4 than as the main business segmentation layer.

## Performance design

The dashboard is designed for sub-100ms interactions by avoiding full-dataset computation inside Dash callbacks.

The key design choices are:

1. Use parquet and CSV outputs generated by Phase 1-4.
2. Convert them into small dashboard cache parquet files.
3. Load those cache files once when the app starts.
4. Render most charts from precomputed summary tables.
5. Limit transaction-level display to the exported suspicious transaction queue, not the full 6.3M rows.

Actual latency still depends on laptop hardware and browser performance. The architecture avoids expensive full-table filtering during interaction.

## Expected project structure

Place this folder inside the project root, next to `phase_1`, `phase_2`, `phase_3`, `phase_4`, and `datasets`.

Expected generated files:

```text
datasets/
  phase_1/
    paysim-dataset.parquet
  phase_2/
    paysim-dataset-phase2.parquet
    paysim-outliers-phase4.parquet
    cluster-summary-phase2.csv
  phase_3/
    top_10_final_rules.csv
    report_worthy_rules.csv
    fraud_focused_rules.csv
  phase_4/
    paysim-suspicious-transactions.parquet
    risk_summary.csv
    anomaly_type_summary.csv
    investigation_summary.csv
    fraud_by_score.csv
    method_overlap.csv
    cluster_kmeans_risk_summary.csv
    fraud_validation_metrics.csv
```

## Installation

```bash
cd phase_5_dashboard
pip install -r requirements.txt
```

## Build dashboard cache

Run this after Phase 1-4 have been executed and their parquet/CSV outputs exist.

```bash
python build_dashboard_cache.py --project-root ..
```

The script writes compact files into:

```text
phase_5_dashboard/cache/
```

## Run dashboard

```bash
python app.py
```

Then open:

```text
http://127.0.0.1:8050
```

## Fallback mode

If no dashboard cache exists, the app still opens using summary values extracted from the executed notebook outputs embedded in the submitted project archive. For final submission, build the cache from the local parquet outputs so the dashboard reflects the latest run.

## Business wording choices

- `High risk` is described as a review-priority queue, not a final fraud prediction.
- Association rules use plain-English labels: `coverage`, `hit rate inside the group`, and `uplift vs normal`.
- Customer segmentation is described as customer-transaction behavior segmentation because the unique account IDs were removed during preprocessing.
- Anomalies are framed as operational categories: possible data quality issue, rare legitimate transaction, potential risk/monitor, or potential fraud.
